import { Input } from "@chakra-ui/react";
import React from "react";

const SearchInput = () => {
  return <Input borderRadius={20} placeholder="Search Gnmes..." variant={"fi"} />;
};

export default SearchInput;
